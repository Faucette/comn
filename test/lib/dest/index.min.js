(function(dependencies, modules, global) {
    var cache = {};

    function require(path) {
        var module = cache[path],
            callback, exports;

        if (module === undefined) {
            callback = dependencies[path];
            exports = {};

            cache[path] = module = {
                exports: exports,
                require: require
            };

            callback.call(exports, require, exports, module, global);
        }

        return module.exports;
    }

    require.resolve = function(path) {
        return path;
    };

    

    require.async = function(index, callback) {
        var module = cache[index],
            path, node;

        if (module) {
            callback(module.exports);
            return;
        }

        path = modules[index];
        node = document.createElement("script");

        function onload() {
            callback(require(index));
        }

        node.type = "application/javascript";
        node.charset = "utf-8";
        node.async = true;

        if (node.attachEvent) {
            node.attachEvent("onreadystatechange", onload);
        } else if (node.addEventListener) {
            node.addEventListener("load", onload, false);
        } else {
            node.onreadystatechange = onload;
        }

        node.src = path;
        document.head.appendChild(node);
    };

    global.__define__ = function(index, moduleDependencies) {
        var key;
        if (modules[index]) {
            for (key in moduleDependencies) {
                if (!dependencies[key]) {
                    dependencies[key] = moduleDependencies[key];
                }
            }
        }
    };

    

    if (typeof(define) === "function" && define.amd) {
        define([], function() {
            return require(0);
        });
    } else if (typeof(module) !== "undefined" && module.exports) {
        module.exports = require(0);
    } else {
        
        global.lib = require(0);
        
    }
}({
0: function(require, exports, module, global) {

var app = require(1);


app.init();


var add = require(5);
console.log(add(10, 5));


},
1: function(require, exports, module, global) {

var folder = require(2);


var app = exports;


app.init = function() {
    require.async(4, function(math) {
        var test = require(7);

        require.async(8, function(mod) {
            mod.set("asdf");
            console.log(mod.get());
        });

        console.log(math, test(true));
    });
};


console.log(folder);


},
2: function(require, exports, module, global) {

var folder = exports;


folder.file = require(3);


},
3: function(require, exports, module, global) {

var __dirname =  module.dirname = "./folder";
var __filename = module.id = module.filename = "./folder/file.js";
var file = exports;


file.name = __filename;
file.directory = __dirname;


},
5: function(require, exports, module, global) {

module.exports = function add(a, b) {
    return a + b;
};


}}, {
4: "dest/math_index.min.js",
8: "dest/mod.min.js"}, (new Function("return this;"))()));
